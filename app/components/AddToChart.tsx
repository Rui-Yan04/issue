'use client'
import React from 'react'

const AddToChart = () => {
  return (
    <div>
      <button onClick={() => console.log('add to chart')}>Add to chart</button>
    </div>
  )
}

export default AddToChart
